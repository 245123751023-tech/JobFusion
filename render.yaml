services:
  - type: web
    name: jobfusion
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn jobfusion.wsgi:application"
    envVars:
      - key: DEBUG
        value: False
      - key: SECRET_KEY
        generateValue: true